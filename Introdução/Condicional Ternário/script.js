let age = 19;

let isAdult = (age > 18 ? true : false);

console.log('É ADULTO?', isAdult);

let isMember = true;
let shipping = (isMember ? 2 : 10);
console.log('Quanto tá o frete?', shipping);
